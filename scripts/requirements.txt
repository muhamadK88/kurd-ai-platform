# Kurd AI — automated news pipeline dependencies
# Install:  pip install -r scripts/requirements.txt

feedparser>=6.0.11        # RSS/Atom parsing
news-please>=1.6.13       # full article body + lead-image extraction
requests>=2.32.0          # HTTP: image validation + POST to the website
google-genai>=1.10.0      # Gemini 2.5 Pro (fact-check + bilingual translation)
python-dateutil>=2.9.0    # tolerant timestamp parsing across feeds
